# Copy this file to .env.development at the repository root for local development.
# All local development commands read that one root file. Never commit the copied file.

# Shared Sanity project and development dataset.
PUBLIC_SANITY_PROJECT_ID=your-project-id
PUBLIC_SANITY_DATASET=development
SANITY_STUDIO_PROJECT_ID=your-project-id
SANITY_STUDIO_DATASET=development

# Needed only when the development dataset is private. This is server-only: never
# rename it with PUBLIC_ or SANITY_STUDIO_, because those prefixes expose values.
SANITY_API_READ_TOKEN=

# Church Main integrations.
# In Breeze, open the form's Share Form menu and copy its Form Address or iframe src URL.
PUBLIC_NEW_TO_CHURCH_FORM_URL=https://YOUR_SUBDOMAIN.breezechms.com/form/YOUR_FORM_SLUG

# Verified newsletter preference workflow. Every value below is server-only; do not
# add PUBLIC_ or SANITY_STUDIO_ prefixes. Copy them into the root .env.development
# for local handler tests and into both Vercel projects for deployed API functions.
NEWSLETTER_ALLOWED_ORIGINS=http://localhost:4321,http://localhost:1234
NEWSLETTER_VERIFICATION_SECRET=replace-with-at-least-32-random-characters
NEWSLETTER_VERIFICATION_TTL_SECONDS=900
NEWSLETTER_REVIEW_TTL_SECONDS=604800
NEWSLETTER_IP_RATE_LIMIT=5
NEWSLETTER_IP_RATE_WINDOW_SECONDS=900
NEWSLETTER_EMAIL_RATE_LIMIT=3
NEWSLETTER_EMAIL_RATE_WINDOW_SECONDS=3600
NEWSLETTER_BREEZE_OPERATION_RATE_LIMIT=8

# Breeze server-side API connection and matching fields. IDs are preferred in
# production. When the four field/option IDs are blank, the integration discovers
# one exact match using the canonical names below and fails closed on duplicates.
BREEZE_SUBDOMAIN=YOUR_SUBDOMAIN
BREEZE_API_KEY=
BREEZE_EMAIL_FIELD_ID=
BREEZE_NEWSLETTER_PREFERENCE_FIELD_ID=
BREEZE_NEWSLETTER_SUBSCRIBE_OPTION_ID=
BREEZE_NEWSLETTER_UNSUBSCRIBE_OPTION_ID=
BREEZE_EMAIL_FIELD_NAME=Email
BREEZE_NEWSLETTER_PREFERENCE_FIELD_NAME="Newsletter communication preference"
BREEZE_NEWSLETTER_SUBSCRIBE_OPTION_NAME="Subscribe me to church newsletters"
BREEZE_NEWSLETTER_UNSUBSCRIBE_OPTION_NAME="Do not send me church newsletters"
# Set true only after the client approves creating a minimal verified subscriber
# when no Breeze profile matches. Unsubscribe requests never create profiles.
BREEZE_CREATE_MISSING_SUBSCRIBERS=false

# Encrypted one-time token, review queue, and rate-limit storage. Vercel/Upstash
# may provide UPSTASH_REDIS_REST_* or the legacy KV_REST_API_* names.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Transactional verification and staff-review email delivery through Resend.
RESEND_API_KEY=
NEWSLETTER_FROM_EMAIL="RCCG Cornerstone Assembly <news@YOUR_VERIFIED_DOMAIN>"
NEWSLETTER_REVIEW_EMAIL=connect@rccgcornerstoneassembly.com

# Blueprint CLI connection. The configure helpers provide RCCG development defaults,
# but keeping these here also lets direct Blueprint commands evaluate the manifest.
SANITY_PROJECT_ID=your-project-id
SANITY_DATASET=development
SANITY_BLUEPRINT_STACK_ID=

# Development Vercel hook URLs. This file's .env.development copy is ignored by Git.
# The configure helpers read these values and synchronize them to the hosted Function.
CHURCH_MAIN_VERCEL_DEPLOY_HOOK_URL=
WOMAN_EXCEL_VERCEL_DEPLOY_HOOK_URL=
